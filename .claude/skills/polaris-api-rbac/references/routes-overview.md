# API route groups — overview

The route groupings under `/api/v1/` as CLAUDE.md summarized them (route file in `src/api/routes/`). Full per-endpoint bodies, shapes and gates are in the `endpoints-*.md` files beside this one.

All routes are prefixed `/api/v1/` and aggregated by `src/api/router.ts`. Permission gates live in `src/api/middleware/permissions.ts` — `requirePermission(functionKey, level)` resolves against the caller's role snapshot (session-stamped for browser callers; resolved from the token's bound Role for bearer callers), with ownership-aware `requireOwnership(functionKey)` for `subnets` / `reservations`.

**Three endpoints live OUTSIDE `/api/v1` and outside the permission model**, declared directly on the app in `src/app.ts` and gated only by an optional bearer token from `.env`:

- `GET /health` — liveness. Answers 200 `{status:"ok"}` whenever the process is up and **checks nothing at all**: the first-run setup wizard polls it from localhost before a database exists, so a database check here would deadlock provisioning. Gated by `HEALTH_TOKEN` when set.
- `GET /health/ready` — readiness. 200 `{status:"ready"}` only when the local PostgreSQL is a **writable primary**; otherwise 503 `{status:"not-ready", reason}` with reason `in-recovery` (this node is a hot standby — the expected answer on the standby of an active/standby pair), `db-error` or `timeout`. `Cache-Control: no-store`. Same `HEALTH_TOKEN` gate. Backed by `utils/readinessCheck.ts` on its own one-connection pool over the DIRECT database url, never the Prisma pool: a saturated application pool must not be able to flap a healthy site out of a load balancer. This is the endpoint a load balancer should monitor — see `docs/HA.md`.
- `GET /metrics` — Prometheus. Gated by `METRICS_TOKEN` when set, and additionally by an nginx `allow` block in production.

None of the three carry a session, a role snapshot or a `requirePermission` gate, and all three are served only by roles with `runsHttp` (`web` / `all`) — `monitor`, `discovery` and `dash` answer none of them. When adding an app-level endpoint, decide deliberately which side of `/api/v1` it belongs on: everything on the router inherits CSRF, the session/bearer resolution and a permission gate, and these three deliberately do not.

Resource groupings (route file in `src/api/routes/`):

- **auth** (login / logout / Azure SSO / TOTP)
- **blocks** / **subnets** / **reservations** (IP-space CRUD + per-subnet refresh + stale-reservation alerts + queued-push lifecycle + the **exclusion registry** at `GET /subnets/exclusions` (`subnets:read`) + `POST|PUT|DELETE /subnets/exclusions[/:id]` (`subnets:**fullwrite**`, no `cidr` on the PUT — business rule 42) + the **subnet archive**: `GET /subnets/archived` and `GET /subnets/archived/:id` at `subnets:read`, and `POST /subnets/:id/archive` at `subnets:**fullwrite**` (surfaced as **Archive** in the Networks row menu) — retiring a site's address space is not an own-rows action, and a discovered subnet's `createdBy` is null so the ownership tier could never reach one. The two reads MUST stay declared above `GET /:id`. Business rule 41)
- **utilization** / **dashboard** (reporting)
- **users** / **roles** / **apiTokens** / **credentials** (identity + access. Credential reads are `credentials:read`; the four mutations — POST, PUT, DELETE and **POST /credentials/test** — go through `requireOwnership("credentials")` + `assertOwnership`, so `write` reaches only rows the caller created and `fullwrite` reaches any. `/test` is in that set because passing a stored `id` merges the row's real secrets into the probe; testing an unsaved form needs only `write`. Business rule 43)
- **groupMappings** (`/group-mappings`, IdP group → role/tags map; gated `users=fullwrite`)
- **userDashboard** (`/me/dashboard`, per-user dashboard layout CRUD) and **notificationPreference** (`GET|PUT /me/notification-preference` — the caller's OWN alert delivery preference: `email` | `push` | `any`. Strictly per-caller like its two `/me/*` siblings, gated `alerts:read` on both verbs — the same gate the push-subscription routes carry, since choosing a delivery method is not an alert-management privilege. The GET also returns `options` so both clients render the server's vocabulary rather than each hardcoding labels; it additionally rides `GET /auth/me`, so a client reconciles this browser's push enrollment on the boot request it already makes. Business rule 39)
- **savedDashboards** (`/saved-dashboards` — named dashboard canvases, private or public; gated by the `savedDashboards` key with `write` required per request when `visibility:"public"`, `fullwrite` to delete someone else's, and the read floor declared INSIDE the route file because the router is mounted TWICE: once here and once on the Dash wallboard listener, where there is no session (PUBLIC rows only) and every write verb 405s app-wide. `GET /:id` is declared LAST and answers **404, not 403**, for a row the caller can't see — the `GET /alerts/:id` posture, since an anonymous wallboard viewer must not learn that an id exists)
- **savedFilters** (`/saved-filters` — named table filter+sort presets for a list page, private or shared; session callers only. The gate is resolved PER REQUEST from the payload's `scope` rather than at the mount, since the function key depends on which table the preset belongs to — the shared resolver is `api/middleware/scopeAccess.ts`) and **tableTabs** (`/me/table-tabs` — the caller's own view tabs for one table; read-level gate on both verbs) and **userTimezone** (`GET|PUT /me/timezone` + `POST /me/timezone/detected` — the caller's OWN display timezone. The one `/me/*` route with NO permission gate at all: what zone a timestamp is drawn in changes nothing about which data an account can reach, so any function-key gate would leave some legitimate signed-in user unable to fix their own clock. The detected-zone POST is what lets the `auto` default mean the operator's wall clock in an alert EMAIL, which has no browser to ask)
- **integrations** (FMG / FortiGate / Entra ID / AD / Windows Server / vCenter / Azure Arc config + discovery + auto-monitor preflight + per-type Query API proxy)
- **assetTypes** (registry CRUD, mounted before `/assets`; plus the matching surface — `GET /match-schema` (vocabulary + the authoritative paths rules cannot reach), `POST /match-preview` (read-level dry run over the `other` bucket, optionally against an UNSAVED draft substituted into the live registry), `POST /match-apply` (`assets:write`, the retroactive step, audited) — all three declared before `/:id`)
- **assets** (CRUD + monitor history endpoints + `GET /assets/quarantine-availability` (is `config.pushQuarantine` on for ANY enabled Fortinet integration? `assetsQuarantine:read`, MUST stay above `GET /:id` — every surface that OFFERS a quarantine reads it and withholds the verb when false, since `quarantineAsset` skips push-disabled integrations and would 502 with "0/0 FortiGate(s) accepted the push"; Release is never gated on it) + `GET /assets/:id/metric-thresholds?metric=&sensorName=&sensorClass=` (the severity tier ladder that would fire on a charted metric — feeds the charts' severity shading; `assets:read`) + per-asset agent install (**`assets:fullwrite`** — install / retry / reinstall / upgrade / uninstall + `POST /assets/bulk-agent-install`; deploying runs an installer on the host over a stored SSH/WinRM credential, which is not the same act as editing an inventory row. `GET /:id/agent` and `GET /assets/agent-install-scripts` stay at `read`; the client withholds the verbs via `canDeployAgent()` / `data-deploy-agent`. Business rule 43) + dependencies + quarantine + system-info + `GET /assets/:id/arp-table?range=current|1h|12h|24h|7d|30d` (the FortiGate's neighbour cache over that window, plus the device's own poll cadence and the configured retention so the tab can state both; `assets:read`) + `GET /assets/ip-context?ip=` (everything already known about ONE address — containing network, the lease/reservation on it, the gates whose ARP tables and sightings have resolved it, the switch port its MAC was learned on, any asset already carrying it, plus an advisory prefill; read-only, gated `assets:read` with the two IPAM halves gated again on the caller's own `subnets`/`reservations` read and reported in `visibility`; MUST stay above `GET /:id`; see `services/ipContextService.ts`) + `GET /assets/:id/upstream` (the three upstream devices the General tab names — Last Seen Switch / AP / Firewall — resolved from the display strings discovery stores to the Asset rows behind them, so those rows carry verbs (open the device, open its HTTPS UI, SSH to it) instead of being text an operator re-finds by hand. Resolution rides `utils/fortinetParentKey.ts`, never a hostname match, and an unresolved name comes back as the NAME with `asset: null` — a legitimate state, not an error; the firewall half reads `AssetFortigateSighting` and is therefore gated a second time on the caller's own `assetsQuarantine:read` with the answer reported in `visibility`, the `/ip-context` precedent; `assets:read`, see `services/assetUpstreamService.ts`) + SNMP walk + single-FortiGate re-discover + the **Mass Pinning** trio: `GET /assets/pin-filter-schema` (the condition-builder vocabulary, `assets:read` — its own route rather than the `automationManagement:read`-gated `/automations/schema`, the `/contacts/filter-schema` precedent, built from the same shared `scopeConditionMeta` so they can't drift; MUST stay above `GET /:id`), `POST /assets/pin-inventory` (resolves the condition tree server-side via `loadScopeAssetIds` then returns the matched assets' interface/storage inventory aggregated by name with per-device pinned state — `mode:"count"` for the debounced preview, `mode:"full"` for the checklist; a matched set over 1000 answers `overCap` instead of truncating, since "pinned on ALL matched devices" is only honest over the complete set), and `POST /assets/mass-pins` (`assets:write` — pin/unpin DELTAS, one `asset.pins.bulk_updated` Event, per-asset skip-with-reason on the 64-pin cap); see `services/massPinService.ts`)
- **agents** (`/agents`, `/agents/enroll`, `/agents/binary` — Polaris Agent enroll / config / sample-push / binary download, gated by `requireAgentBearer`; the `/agents/ws` WebSocket upgrade handler in `agentsWs.ts` is attached at the HTTP-server level in `src/app.ts`, not via the REST router)
- **ha** (`/ha` — the Server Settings → High Availability tab, docs/HA.md. **TWO routers with opposite auth models.** `haRouter` is a session surface mounted after `requireAuth`, ABOVE `/server-settings` so "ha" is never captured as a settings sub-path: `GET /status` (config + live Patroni cluster + enrollments) and `POST /advisories` (the guidance card, separate because it runs socket probes and shell-outs) at `serverSettingsSystem:read`; `POST /enable`, `POST /disable`, `POST /tokens`, `POST /enrollments/:id/approve`, `POST /enrollments/:id/reject` and `GET /teardown-script` at **fullwrite**, because each hands over or revokes the keys to the install. `POST /tokens` mints and returns the bootstrap script in ONE call — the raw token exists only in that response, so there is no later download to gate. `GET /status` returns `redactHaConfig`, which reports presence flags and never a value; an integration test asserts no PEM and no `privateKey` appears in it. `haEnrollRouter` is mounted at `/ha/enroll` BEFORE `requireAuth` and is **unauthenticated, rate limited (400 / 5 min) and CSRF-exempt** — a node being built has no session and presents a single-use token in the body, exactly the `/agents/enroll` model. Its whole surface is three calls: `POST /` registers (spending the token, recording the source IP and the SSH host keys presented) and returns an opaque 32-byte `requestId`; `GET /:requestId` polls; `GET /:requestId?download=1` claims the bundle once. **Redeeming a token releases nothing** — an operator must approve the node first, so a leaked script produces a request a human can reject rather than a silent handover of `.env`, the nginx private key and the database passwords. Every rejection returns identical text, and a malformed request id 404s before any query)
- **events** / **conflicts** / **search** (audit + resolution + global typeahead; conflict resolution is `accept` / `reject` / `merge` plus `POST /conflicts/:id/reassign-ip`; the duplicate-ip flavor rides the latter two — `merge` takes a `{ survivorAssetId, absorbAssetIds }` body there — and both are the routes on that mount with a CHAINED gate, `discoveryConflicts:write` AND `assets:write`, since they edit (and, for merge, delete) inventory; scope prefixes `block:`/`b:`, `network:`/`n:`, `asset:`/`a:`, `reservation:`/`r:`, `map:`/`m:`, `tag:`/`t:` — the last matches tag substrings across blocks/subnets/assets)
- **alerts** (`/alerts`, canonical; `/notifications` deprecated alias — list + acknowledge/clear + `GET /:id` (ONE alert, `alerts:read`, region-scoped through the same predicate the list uses and answering **404, not 403**, when the caller's regions exclude it — declared last; it backs `/alert-ack.html`, the page an emailed or pushed Acknowledge button opens. `POST /acknowledge` takes an optional `source` from a closed set — `ack_page` | `web_push_action` — which is audit provenance only, never a gate); per-route gates `alerts:read|write|fullwrite`) and **automations** (`/automations`, canonical; `/notification-rules` deprecated alias — rule CRUD + `/schema` builder vocabulary + `/preview` dry-run + `POST /dimension-values` (the values the DRAFT'S OWN scoped devices currently report for one metric OR state-field dimension — sensor classes / sensor names / interfaces / mounts / SD-WAN health-checks + members + rules / tunnels, plus the device-identifier dimensions' hostnames / IPs / MACs / manufacturers / models, so the builder offers a picker instead of a free-text box; `automationManagement:read`, backed by `notificationDimensionService`, optional `narrow` narrows sensor NAMES to the chosen class and WAN members to the chosen health-check. Each source may declare a **`candidateWhere`** — which assets can REPORT the dimension, applied before the interactive 250-asset cap: without it a fleet-wide scope's blind sample missed the few FortiGates that report SD-WAN and the picker answered "no health checks" about a fleet that has them, prod 2026-08-21) + `POST /dimension-values`'s sibling **`POST /poll-cadence`** (how often the draft's own monitored devices take the reading it watches — `{stream, mode, min, max, timeoutMs, assetCount}` from `notificationCadenceService`, `automationManagement:read`: the wizard counts its hold and window fields in POLLS and needs a cadence to convert to the seconds it still STORES, and to say which cadence it converted at; the down-detection caption reads the same lookup); gated `automationManagement`; POST/PUT with script actions additionally require `automationScripts=fullwrite`) plus `GET /automations/:id/removal-impact` (`automationManagement:read`, declared BEFORE `/:id` — how many devices deleting or disabling this automation would leave with NO down detection at all, i.e. Passive; backs the delete/disable confirmation, which is the ONLY thing that can warn about a fleet going unjudged, since the automation being removed is what would otherwise alert about it; see business rule 36) plus **automationScripts** (`/automations/scripts` — registry CRUD + `/runs` history + per-script server test-run; gated `automationScripts`, mounted before `/automations`). The Automations cutover dual-mounts the pre-rename paths on the same routers with `Deprecation` + successor `Link` headers (`deprecatedAlias` in `router.ts`); keep `/notification-rules` mounted before `/notifications`. Per-asset bundle at `GET /assets/:id/alerts` (alias `/assets/:id/notifications`; active + matching rules). **maintenanceSchedules** (`/maintenance-schedules` — schedule CRUD + `/preview` device-list dry-run + `GET /occurrences?from&to` for the modal's calendar tab (occurrences expanded server-side and returned as server-local wall-clock strings, `maintenanceManagement:read`) + `GET /server-time` (the server's wall clock + IANA zone + offset, `maintenanceManagement:read`, declared before `/:id` — every window is picked in SERVER-local wall clock with no offset, so a browser prefilling a `datetime-local` from its OWN clock posts the operator's digits for the server to read as its own; on a UTC-clocked host with a Central operator that turned "now → now + 2h" into a window that started five hours ago and had ALREADY ENDED, so the schedule saved cleanly and nothing ever entered maintenance. `resolveStartNow` only ever covered the ad-hoc START); gated `maintenanceManagement`; every mutation reconciles inline so ad-hoc windows apply immediately. Per-asset reads on `/assets/:id/maintenance-windows` + `/assets/:id/maintenance-info`, gated `assets:read`). **deliveryChannels** (`/delivery-channels`, canonical; `/notification-channels` deprecated alias — delivery-channel registry CRUD + `/:id/test` + `/:id/generate-vapid`; gated `automationManagement`. Plus `GET|PUT /delivery-channels/web-push` — **Web Push is a single on/off capability, not a destination**: enabling creates the singleton + generates the VAPID keypair in one call, disabling never deletes the keys (every `PushSubscription` is signed against them), and recipients are chosen per Notify action. Declared **before `/:id`** so the literal path isn't captured) and **pushSubscriptions** (`/push-subscriptions` — VAPID `/key` + per-user subscribe/unsubscribe; gated `alerts:read`). Delivery channels are managed on the Automations → Delivery tab. **contacts** (`/contacts` — address-book CRUD, with the LIST **paginated and searched server-side** (`?q=&limit=&offset=` → `{ contacts, total, limit, offset }`, default 50 / max 200; `total` is the UNPAGED match count, so read it rather than `contacts.length`) and `GET /:id` for one row, since re-reading the whole list to find one is wrong once the list is a page + `GET /search` (the unified recipient typeahead: Polaris users ∪ contacts, deduped by address with the user winning) + `POST /preview` (device-filter dry-run); plus `GET /filter-schema` (the device-filter builder's vocabulary + value suggestions — its own route because `/automations/schema` + `/scope-options` are gated `automationManagement:read`, and browsing the address book must not require permission to edit automations; `assetTypes` + `tags` ride the payload for the same reason regions and roles ride `/scope-options`). Reads gated `contacts:read`, the three mutations use `requireOwnership("contacts")` + `assertOwnership` so a write-level caller only edits/deletes rows they created. `/search` + `/preview` are declared before `/:id`).
- **networkScans** (`/network-scans` — saved network **Discovery** CRUD + runs: `GET /` (the Discoveries the caller can see — their own plus every SHARED one — each with its newest run, which is also the reattach path), `POST /preview-targets` (resolve typed targets with the pure `expandScanTargets` + report how many addresses inventory already carries — read-level, no packets), `GET /runs/:runId` (progress + hits), `POST|PUT|DELETE` CRUD, `POST /:id/run` (202 + the run row; the sweep takes minutes and the wizard watches that row), `POST /runs/:runId/cancel`, and `POST /runs/:runId/adopt`. The whole mount carries a **`networkScan:read` floor** (the `/map` precedent); adopt is the one route with a **chained** gate — `networkScan:write` AND `assets:write` — which is the entire permission design: running a Discovery creates nothing, so a role may be allowed to find out what is on a range without being allowed to put it in inventory (business rule 34a). Every route additionally resolves the caller's user id and scopes on **visibility**: reads and `POST /:id/run` need only to SEE the row (running a shared Discovery is what publishing one is for), while `PUT`/`DELETE /:id` are the owner's with `networkScan:fullwrite` reaching anyone's; an invisible row answers **404, not 403**, and `/runs/:runId` inherits its Discovery's visibility. Publishing deliberately costs nothing above `write` — unlike saved filters and dashboards, whose mounts sit at `read` — since `networkadmin` / `assetsadmin` hold `write` and are exactly the roles that author Discoveries (business rule 34g). `/preview-targets` and `/runs/...` are declared before `/:id`. Route schemas are SHAPE only; the semantic rules live in `networkScanService.validateScanInput`, because an imported `.discovery.json` has to pass exactly the same checks).
- **map** / **mapRegions** / **allocationTemplates** (Device Map + map-region polygons + saved subnet allocations. `POST /map/sites/:id/topology/layout/checkpoint` (`deviceMap=write`) is the explicit **Save** — same body as the layout PUT, writing the live blob AND the restore point in one statement so a checkpoint can never be of a layout the server has not stored; `DELETE …/layout` is **Reset to baseline** and keeps a restore point where one exists. The whole `/map` mount carries a **`deviceMap=read` floor** — added 2026-08; the three read routes were previously auth-only, so any authenticated session or bearer token could enumerate every FortiGate's hostname, coordinates and full topology regardless of its role. `PUT|DELETE /map/sites/:id/topology/layout` escalates to `deviceMap=write`; reads ride the topology GET's `savedLayouts` embed. The dash listener mounts the same router behind the same floor, satisfied by the built-in readonly role it answers as)
- **applicationMap** (`/application-map` — the Application Map connectivity graph built from mapped-process AND mapped-service connection facts (`Asset.mappedProcesses` → `process` child nodes, `Asset.mappedServices` → `service` child nodes matched on the connection row's owning `unit`; both render even with zero connections). Only **`monitored: true`** hosts appear as compound parents — stop-monitored / decommissioned / disabled assets drop off the map without clearing their pins (agent removal additionally clears the pins + connection rows); gated `applicationMap:read`; `PUT|DELETE /application-map/layout` persists the shared drag layout, gated `applicationMap:write`. Service/process discovery rules (the list UI lives on **Integrations → Polaris Agent**, not the map page): `GET /application-map/discovery` (the stored rule set + `assetLabels` resolving explicit assetIds to hostnames) plus `POST /discovery/scope-preview` (which assets a scope ∪ explicit assetIds selects), `POST /discovery/inventory` (what those assets report — the scope-driven item picker) and `POST /discovery/preview` (one rule's dry run) are `applicationMap:read`; `PUT /discovery` and `POST /discovery/unmap` require `applicationMap:write` **AND** `assets:write` (chained — they change per-asset pin arrays). Rules carry `mode` (`map` = monitor + Application Map, `monitor` = monitor-only) and `source` (`manual` / `auto` — auto rules are minted/consolidated server-side by the `PUT /assets/:id` pin-diff hook, `recordOperatorPinChanges`). Per-asset Ports & Connections read at `GET /assets/:id/process-connections`, gated `assets:read`)
- **weather** (`/weather` — Status Map weather proxy: RainViewer radar frame list + tile proxy + Open-Meteo temperature, server-side cached; also mounted on the Dash listener. The widget is proxy-first with direct-CDN fallback)
- **serverSettings** (HTTPS, branding, backup/restore + the automatic-backup cadence at `GET|PUT /server-settings/database/backup-schedule`, capacity advisor, sample retention, queue mode, security tokens, Dash wallboard toggle at `GET/PUT /server-settings/dash` — PUT is `serverSettingsSystem=fullwrite`-gated, local-login source-IP restriction at `GET|PUT /server-settings/login-access` — same fullwrite gate, the GET also reports `callerIp` as Polaris resolves it and the PUT refuses a scope excluding the caller; see business rule 31 — and the **API-docs source-IP scope at `GET|PUT /server-settings/api-docs`** (same GET-reports-`callerIp` / fullwrite-PUT pattern; edited on the API Tokens tab). The scope (`apiDocsConfig` Setting, `services/apiDocsAccessService.ts`) gates the unauthenticated `GET /api` developer-docs page: `loopback` | `rfc1918` (default) | `custom`, deliberately no "all", custom entries must sit inside RFC1918 (`isRfc1918Cidr`) and are re-filtered on read, loopback is always allowed while enabled, and the gate in `src/app.ts` (paths `/api`, `/api/`, `/api.html` — the last because `express.static` would serve the file ungated) drops the socket and **fails CLOSED**, the opposite of login-access's fail-open, because this fronts an unauthenticated disclosure surface. The PUT answers `{apiDocs, callerAllowed, nginx}` — being outside the docs scope warns, never refuses, and the nginx field reports the best-effort re-render of the managed `location = /api` allow block (skipped silently unless proxy mode + managedMode + no drift). The backup/restore/schedule routes are thin wrappers over `services/backupService.ts` + `services/backupScheduleService.ts`
- **proxySettings** (`/server-settings/proxy` — in-app nginx GUI config + cert preflight/rotate)
- **tags** (`/server-settings/tags` — the tag registry: CRUD + `/tags/settings` + `POST /tags/preview-criteria` (device-filter dry-run: match count, sample, and the `+add/-remove` delta vs the tag's current auto-assignments — takes either filter shape) + `GET /tags/filter-schema` (the condition builder's vocabulary + value suggestions for a tag's auto-assign filter, `serverSettingsSystem:read`, declared before `/tags/:id`; its own route rather than `/automations/schema` or `/contacts/filter-schema` because managing the tag registry must not require permission to edit automations or browse the address book — the `/assets/pin-filter-schema` precedent — and it carries the WIDE `DEVICE_FILTER_FIELD_OPS` field set because the flat tag builder already offered `osVersion` / `department` / `location` / `fortigate` plus a wildcard operator). `GET /tags` decorates each row with the fold-forward (`assetConditionEffective` + `assetFilterUnconvertible`) so the editor opens a legacy tag in the builder rather than rendering it as unfiltered. Writes are `serverSettingsSystem=fullwrite` and refuse the Device-Map-owned "Map Regions" category. **`GET /server-settings/tags/catalog` is the picker-shaped read and is declared in `router.ts` ABOVE the blanket mount** (the `/server-settings/mibs` precedent), so it is auth-only: the shared tag picker is rendered by every form that can tag something — asset edit, blocks, subnets — and none of those roles hold `serverSettingsSystem`, so reading the catalogue through `GET /tags` 403'd for every non-admin built-in role and `_ensureTagCache`'s swallowed failure made the picker render "No tags defined yet" at an install with a full registry. Lean by design (`listTagCatalog` → id/name/category/colour + the `enforce` flag): a `Tag` row also carries the auto-assignment device filter, which is registry-management detail and stays behind the gate, while name/category/colour is what every asset row already shows anyone with `assets:read`)
- **mibs** (`/server-settings/mibs` and `/server-settings/mibs/std`)
- **manufacturerProfiles** / **manufacturerAliases** / **deviceIcons** (telemetry profile + alias map + topology icons)
